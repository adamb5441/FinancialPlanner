@model FinancialPlanner.Models.Invitation

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    @Html.HiddenFor(x => x.Key)
    <button> accept</button>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
